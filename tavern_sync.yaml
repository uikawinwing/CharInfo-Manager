# yaml-language-server: $schema=https://testingcf.jsdelivr.net/gh/StageDog/tavern_sync/dist/schema/settings.zh.json

# CharInfo-Manager 当前没有需要 tavern_sync 管理的角色卡、世界书或预设。
# 保留空配置，让 webpack watch/build 可以安全调用 tavern_sync，而不会监听模板仓库中已经不存在的示例路径。
配置: {}
