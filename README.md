# 命定之诗角色查看器

一个用于 SillyTavern / 酒馆助手环境的角色信息查看器。它把 `<char_info>`
中的 YAML 角色资料渲染成可阅读的角色面板，并提供导入角色状态与聊天世界书的快捷操作

## 功能

- 后台脚本读取聊天原始消息，并直接识别其中完整的 `<char_info>...</char_info>`
- 渲染角色档案、属性、资源、技能、装备、背包、登神长阶、背景故事和状态效果
- 根据种族与生命层级自动应用主题色、边框和粒子视觉效果
- 属性公式支持点击查看，并提示总值或分项是否异
- 技能、装备和道具效果支持 `效果名: 内容` 与 `微弱要素[名称]: 内容` 这类带方括号的效果名
- 一键导入到角色状态，尽量保留已有角色的好感度与心里话
- 一键导入到聊天世界书，方便把原始角色资料作为聊天世界书条目保存
- 通过常驻悬浮按钮随时查看已经进入当前聊天的 NPC

## 玩家自修复提示

当 YAML 解析失败时，界面会显示：

- 技术错误信息
- 报错行号和列号
- 自动清洗后的定位行与箭头
- 原始输入行，方便对照修改
- 按错误类型生成的修复建议

目前会重点提示这些常见问题：

- 缩进层级错误。
- 列表项 `-` 对齐错误
- 缺少冒号或 `键: 值` 格式错误
- 方括号、冒号、引号导致的未加引号问题
- 重复键名
- 双引号内反斜杠转义错误

## 输入格式

角色资料必须输出完整标签；后台脚本会自行识别标签并渲染，不需要酒馆正则或 `$1` 捕获：

```yaml
<char_info>
姓名: 艾莉诺
生命层级: 第四层级
等级: 13
种族: 人类
技能:
  - 名称: 慈爱之吻
    品质: 史诗
    类型: 主动
    标签: [精神][治疗]
    效果: |
      微弱要素[圣吻之诺]: 此吻无法被闪避或抵抗，必定命中目标
      神圣契约: 为友方目标恢复生命值，并施加祝福。
</char_info>
```

### 按姓名读取 CharInfo 角色视觉资料

查看器使用聊天级 `char_info.profiles[角色姓名]` 作为自己的视觉资料路径，不再读取或写入
`status.externalGalleries`。头像仍可单独写入状态栏路径；头像与全身立绘分开配置，避免把构图不合适的相册图片自动裁成头像：

```ejs
<%_
const characterName = '角色姓名';
const avatarUrl = 'https://example.com/avatar-crop.webp';
const gallery = [
  { title: '主立绘', sources: ['https://example.com/character-main.webp'] },
  { title: '日常服', sources: ['https://example.com/character-alt.avif'] },
];

setLocalVar(`char_info.profiles[${JSON.stringify(characterName)}]`, {
  schema_version: 1,
  custom_racecolor: '#78C8F0',
  custom_tiercolor: '#A855F7',
  '登场台词': '用一句话留下角色的第一印象。',
  gallery,
});
setLocalVar(
  `status.externalAvatars.partners[${JSON.stringify(characterName)}].url`,
  avatarUrl,
);
_%>
```

LLM 只需在 `<char_info>` 内准确输出姓名，不需要输出图片字段：

```yaml
姓名: 角色姓名
```

查看器会按姓名读取
`char_info.profiles.<姓名>.gallery`。数组第一项固定作为主立绘；存在多张图片时，首页显示左右切换按钮。每张图片的
`sources`
是同一图片的镜像来源：按数组顺序加载，当前地址加载失败时自动尝试下一个。找不到有效相册时回退到无图普通版。状态栏头像只读取
`status.externalAvatars.partners.<姓名>.url`，不会自动拿相册图片代替。

共享给状态栏的动画建议使用动态 WebP 或 AVIF。查看器自身仍兼容静态图片、GIF、动态 WebP／AVIF，以及旧资料中的 MP4／WebM；Catbox 的动画图片与视频会保留原始 URL，避免图片代理压平动画帧或破坏视频流。

`登场台词` 与两种颜色均可省略。颜色只接受 `#RRGGBB`，缺失或无效时自动使用种族与生命层级的默认颜色。

旧版
`char_info_visuals[姓名].url/gallery`、`角色图片: '[[变量名]]`、直接填写图片 URL，以及“占位符对应 URL字符串”的方式仍然兼容。升级后会把
`status.externalGalleries` 中尚未拥有新版图库的角色图片复制到 `char_info.profiles`；旧数据不会删除，已有新版图库也不会被覆盖。之后请使用角色视觉配置管理器维护图片。

变量职责固定如下：

- `chat`：`char_info.profiles` 与状态栏头像；每个聊天只存一份，新聊天自动清空。
- `message`：MVU 剧情快照、关系表和 DX 已登场标记；每个 Swipe 独立，便于重生成与回滚。
- `character`：不保存 NPC 视觉库，避免不同聊天共享旧资料。
- `script`：只保存查看器界面偏好，例如活跃渲染楼层数和粒子特效开关；不保存剧情或角色资料。
- `global`：不保存 CharInfo 项目数据。

豪华／DX 版不读取上述公版相册触发，只接受 `__char_info_ref: special_npc_...`，并从世界书禁用条目
`char_info_special_profiles` 读取专属资料。

### 世界书角色库与角色视觉编辑器

主运行脚本 `dist/char_info_viewer_runtime/index.js` 会在当前聊天角色悬浮列表标题旁提供书本按钮；点击后打开全屏管理界面，并可在其中切换“世界书角色库”和“角色视觉编辑器”。不再依赖酒馆脚本栏里的 QR 按钮。

从 v3.1 升级时，请移除旧的 CharInfo 正则／`$1` iframe 入口和独立的
`dist/char_info_creator_manager/index.js`，只保留 `dist/char_info_viewer_runtime/index.js`。旧管理器脚本在检测到新版运行脚本后不会再打开窗口，避免重复；但它不再是新版安装所需组件，应在下次编辑脚本列表时删除。

- 读取酒馆中的全部世界书，并将当前角色卡绑定的主世界书与附加世界书置顶。
- 在可搜索选择器中查找目标世界书，再读取其中的角色条目。
- “世界书角色库”会显示带有 `[DLC][角色]` 标签的角色条目；没有图片或视觉资料的角色也会显示。
- 没有视觉资料的角色使用占位封面并标记“未配置图片”；有头像时优先使用头像，之后才尝试主立绘及备用图片地址。
- 角色库支持按姓名搜索，并按“当前聊天已遇到”、启用状态和种族筛选。
- 可直接启用或禁用角色条目；开关按世界书名称与条目 UID 定位，只修改目标条目的 `enabled` 字段。
- 点击封面会先打开角色详情，展示完整图库与条目正文；视觉资料与原始正文分开显示。
- 详情页可进入“编辑设定正文”模式；保存只更新目标条目的正文，不影响视觉资料、启用状态和其他设置。
- 没有视觉资料的角色同样可以查看和编辑原始条目正文，也可以从详情页进入五步编辑器新增视觉资料。
- 条目显示名会移除固定 `[DLC][角色]` 标签与末尾括号元数据；世界书中的原始条目名不会被改写。
- 在同一个搜索选择器中筛选角色条目；`[DLC][角色]` 前缀条目会优先显示。
- 编辑姓名、头像、相册标题、每张图片的首选/备用图床 URL、主题色和登场台词。
- 默认将全部图片随角色资料保存，不额外创建图库世界书。
- 可选启用“独立扩展图库”：前三张基础图片仍随角色条目保存，其余图片写入一个可由多个角色共享的禁用 Gallery Pack 世界书。
- 扩展图库只保存引用信息，大型图库不会复制到聊天内容；缺少图库包时基础图片仍可显示。
- 相册步骤提供 Catbox 与 ImgBB 上传页快捷入口；上传仍由用户在第三方图床完成，管理器只接收和校验 HTTPS 直链。
- 旧管理器生成的单 URL 图片会在打开时自动迁移成 `sources` 数组，保存后使用新版格式。
- 主题颜色默认关闭；不启用时不会写入自定义颜色，查看器会使用角色资料的默认主题。
- 世界书条目标题只用于决定写入位置；角色姓名始终使用表单中填写的姓名。
- 世界书角色库可在紧凑列表与图片卡片间切换；图片卡片可选择每行列数。
- 查看器设置可按行填写图片源域名或完整图片 URL，查看时会优先尝试匹配的图床；这不会改写角色条目中的原始图片顺序。
- 切换聊天时管理器会关闭；重新打开后再读取新角色绑定世界书，避免把旧聊天中的未保存编辑带入新上下文。

### 当前聊天角色资料库

`dist/char_info_viewer_runtime/index.js` 会提供当前聊天角色悬浮入口。打开悬浮角色列表后：

- 列表会显示当前聊天中已出现的角色，不建立额外角色副本。
- 支持按姓名、种族和身份搜索，并使用“全部 / 在场 / 不在场”筛选角色。
- 列表显示 `♡ 好感度`；好感度改变时，悬浮按钮与对应角色会显示未读红点，打开该角色后清除。
- 角色详情处于只读模式，不显示导入按钮，并以当前聊天中的“心里话”取代登场台词位置。
- 切换聊天会关闭面板、清空未读状态并重建角色列表。
- “角色视觉编辑器”用于编辑头像 URL、相册主/备用图床 URL、自定义颜色、登场台词与可选扩展图库。
- 角色列表右上角的设置可把显示楼层数设为 1–20，并即时开关粒子特效。

管理器只更新角色视觉资料，不会修改条目原有设定、启用状态或其他世界书参数。发现资料格式异常时会暂停自动写入，避免误删原有内容。

## 开发说明

- 共享角色卡界面源码在 `src/char_info_viewer`
- 纯后台脚本入口在 `src/char_info_viewer_runtime`
- 创作者管理器源码入口在 `src/char_info_creator_manager`
- 后台脚本产物在 `dist/char_info_viewer_runtime/index.js`

新版只使用后台脚本。旧版 CharInfo 正则及其 `$1` iframe 替换入口不再使用。

常用命令：

```bash
npm run build:dev
npm run build
```

## 使用与发布提醒

本项目开源供学习、参考和个人自用。若需要基于本项目进行使用、复刻、DIY 改版并公开发布，请在发布前先告知我，避免版本来源、维护责任和衍生内容说明产生误会。
